# Core dependencies
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0

# Visualization
matplotlib>=3.7.0
folium>=0.14.0

# Geospatial (optional but recommended)
geopandas>=0.13.0
shapely>=2.0.0

# Video processing
opencv-python>=4.8.0

# Optional: Meta SAM 2 for accurate crowd / object detection
# Install CPU-only (laptop):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   pip install git+https://github.com/facebookresearch/sam2.git
# Install with CUDA:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   pip install git+https://github.com/facebookresearch/sam2.git
# Checkpoint: download sam2.1_hiera_tiny.pt from
#   https://dl.fbaipublicfiles.com/segment_anything_2/092824/sam2.1_hiera_tiny.pt
#   and place at walkability_analyzer/video_processing/checkpoints/sam2.1_hiera_tiny.pt
# SAM2 is auto-detected at runtime once installed + checkpoint present.
# torch>=2.3.1
# torchvision>=0.18.0
# sam2  (install from git, see above)

# Audio processing (for clap detection)
librosa>=0.10.0
soundfile>=0.12.0

# Optional: Machine learning for anomaly detection
scikit-learn>=1.3.0

# Development tools (optional)
pytest>=7.4.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.4.0
